name: Build APK
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: |
        sudo apt update
        # Removed libtinfo5 which caused the previous failure
        sudo apt install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev
        pip install --upgrade pip
        pip install buildozer cython
    - name: Build with Buildozer
      run: |
        buildozer init
        # Update spec file with your requirements
        sed -i 's/^requirements =.*/requirements = python3,kivy,kivymd,requests,pyrebase4,plyer,urllib3,charset-normalizer,idna,certifi/' buildozer.spec
        buildozer android debug
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: package
        path: bin/*.apk
